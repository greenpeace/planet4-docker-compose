version: '3'
services:
  traefik:
    image: traefik:1.4
    command: --web --docker --docker.domain=planet4.dev --docker.watch
    dns:
      - ${DNS_RESOLVER:-8.8.8.8}
    networks:
      - proxy
    ports:
      - "80:80"
      - "443:443"
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./traefik.toml:/traefik.toml
    labels:
      traefik.backend: "traefik"
      traefik.frontend.rule: "Host:traefik.planet4.dev"
      traefik.docker.network: "planet4dockercompose_proxy"
      traefik.enable: "true"

  redis:
    image: redis:4-alpine
    networks:
      - local
    volumes:
      - ./persistence/redis:/data

  db:
    image: mysql:5.7
    env_file:
      - ./db.env
    volumes:
      - ./persistence/mysql:/var/lib/mysql:cached
    networks:
      - db

  app:
    image: gcr.io/planet-4-151612/p4-gpi-app-dev:${APP_BUILD_TAG:-develop}
    depends_on:
      - db
      - redis
    networks:
      - local
      - db
    dns:
      - ${DNS_RESOLVER:-8.8.8.8}
    volumes:
      - ./persistence/app:/app/source/public:cached
      - ./secrets/wp-stateless-media-key.json:/app/secrets/wp-stateless-media-key.json:ro
    environment:
      - APP_ENV=${APP_ENV:-develop}
      - APP_HOSTNAME=${APP_HOSTNAME:-test.planet4.dev}
      - NEWRELIC_LICENSE=${NEWRELIC_LICENSE:-}
      - WP_SITE_HOME=${APP_HOSTNAME:-test.planet4.dev}
      - WP_SITE_URL=${APP_HOSTNAME:-test.planet4.dev}
    env_file:
      - ./app.env
      - ./db.env

  openresty:
    image: gcr.io/planet-4-151612/openresty:${OPENRESTY_BUILD_TAG:-develop}
    depends_on:
      - app
      - traefik
      - redis
    networks:
      - local
      - proxy
    volumes:
      - ./persistence/app:/app/www:cached
    env_file:
      - ./app.env
    environment:
      - APP_ENV=develop
      - OPENRESTY_FASTCGI_BACKEND=${OPENRESTY_FASTCGI_BACKEND:-app:9000}
      - PAGESPEED_ENABLED=${PAGESPEED_ENABLED:-false}
      - PHP_ENABLED=true
      - APP_HOSTNAME=test.planet4.dev
    labels:
      traefik.backend: "test"
      traefik.frontend.rule: "Host:test.planet4.dev"
      traefik.docker.network: "planet4dockercompose_proxy"
      traefik.enable: "true"

  phpmyadmin:
    image: phpmyadmin/phpmyadmin:4.7
    depends_on:
      - db
      - traefik
    env_file:
      - ./db.env
    volumes:
      - ./persistence/pma:/sessions
    networks:
      - db
      - proxy
    labels:
      traefik.backend: "pma"
      traefik.frontend.rule: "Host:pma.planet4.dev"
      traefik.docker.network: "planet4dockercompose_proxy"
      traefik.enable: "true"

  smtp:
    image: gcr.io/planet-4-151612/exim:${SMTP_BUILD_TAG:-develop}
    networks:
      - local
    environment:
      - APP_ENV=develop
    labels:
      traefik.enable: "false"


networks:
  local:
  db:
  proxy:
    driver: bridge
